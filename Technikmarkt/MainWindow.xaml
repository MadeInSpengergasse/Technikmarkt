<Window x:Class="Technikmarkt.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Technikmarkt"
        mc:Ignorable="d"
        Title="Technikmarkt Overview" Height="450" Width="700" MinHeight="250" MinWidth="550">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </Window.Resources>

    <Grid Background="DarkGray">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Background="LightGray">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="6*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <!--<TextBox Grid.Column="0" x:Name="suchfeld" Margin="6 7 3 5"></TextBox>-->
            <Grid Grid.Column="0" Background="White" Margin="6 7 3 5">
                <TextBlock VerticalAlignment="Center" Margin="4 0 0 0" Text="Anbieter- oder Händlername..." Foreground="Gray" 
                       Visibility="{Binding ElementName=suchfeld, Path=Text.IsEmpty, Converter={StaticResource BooleanToVisibilityConverter}}" />
                <TextBox x:Name="suchfeld" Background="Transparent"/>
            </Grid>
            <Button Grid.Column="1" x:Name="suchfeld_button" IsDefault="true" Click="suchfeld_button_click" Content="Suchen" Margin="0 7 6 5"/>
        </Grid>

        <Rectangle Grid.Row="1" HorizontalAlignment="Stretch" Fill="DarkGray" Height="2"/>

        <Grid Grid.Row="2" DataContext="{Binding Source={StaticResource vm}}" Background="LightGray">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Anbieter" TextAlignment="Left" FontSize="24" Margin="6 0 3 0" Grid.Column="0" Grid.Row="0"/>
            <ListBox x:Name="Listbox_anbieter" Margin="6 0 3 0" Grid.Column="0" Grid.Row="1"
                 ItemsSource="{Binding AlleAnbieter}"
                 DisplayMemberPath="a_anbietername"
                 SelectedValuePath="a_anbietername">
                <ListBox.SelectedValue>
                    <Binding Path="GewaelterAnbieter"
                         Mode="OneWayToSource"
                         BindsDirectlyToSource="True" />
                </ListBox.SelectedValue>
            </ListBox>

            <TextBlock Text="Händler"  TextAlignment="Left" FontSize="24" Margin="3 0 6 0" Grid.Column="1" Grid.Row="0"/>
            <ListBox x:Name="Listbox_haendler" Margin="3 0 6 0" Grid.Column="1" Grid.Row="1"
                ItemsSource="{Binding AlleHaendler}" 
                DisplayMemberPath="h_haendlername"
                 SelectedValuePath="h_haendlername">
                <ListBox.SelectedValue>
                    <Binding Path="GewaelterHaendler"
                         Mode="OneWayToSource"
                         BindsDirectlyToSource="True" />
                </ListBox.SelectedValue>
            </ListBox>

            <!-- SaveInsertCommand sollte umbenannt werden -->
            <Grid Grid.Column="0" Grid.Row="2" Margin="6 5 3 5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="5*"/>
                    <ColumnDefinition Width="5*"/>
                </Grid.ColumnDefinitions>

                <Button x:Name="sortiment_button" Grid.Column="0" Content="Sortiment" Margin="0 0 3 0"
                Click="sortiment_button_click" IsEnabled="{Binding ElementName=Listbox_anbieter, Path=SelectedItems.Count}"/>

                <Button x:Name="anbieter_information_button" Grid.Column="1" Content="Anbieter Information" Margin="3 0 0 0"
                Command="{Binding SaveInsertCommand}" 
                CommandParameter="{Binding ElementName=Listbox_anbieter, Path=SelectedItem}"/>
            </Grid>

            <Button x:Name="haendler_information_button" Content="Händler Information" Grid.Column="1" Grid.Row="2" Margin="3 5 6 5"
                Click="haendler_button_click" IsEnabled="{Binding ElementName=Listbox_haendler, Path=SelectedItems.Count}"/>
        </Grid>
    </Grid>
</Window>